name: Mengman Detail Probe

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/mengman-bootstrap-probe.yml"

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch signed detail data
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/mengman-detail
          python3 - <<'PY'
          import hashlib,time,urllib.request
          base='https://static.yisix.cn'
          seed='94e890e3a45ba392c0c519ed45190a4c'
          paths=[
            '/static/comic/0/2140.zip',
            '/static/comic/5/2145.zip',
            '/static/comic/40/2140.zip',
            '/static/comic/2140/2140.zip',
            '/static/comic/chapter/2140/0.zip',
            '/static/comic/chapter/2140/1.zip',
            '/comics/0/cover_1776966416457.enc',
          ]
          for path in paths:
              ts=str(int(time.time()))
              sign=hashlib.md5((seed+path+ts).encode()).hexdigest()
              url=f'{base}{path}?ts={ts}&sign={sign}'
              name=path.strip('/').replace('/','__')
              try:
                  req=urllib.request.Request(url,headers={'User-Agent':'okhttp/4.12.0'})
                  with urllib.request.urlopen(req,timeout=30) as r:
                      data=r.read()
                      print(path,'status',r.status,'len',len(data),'type',r.headers.get('content-type'),'head',data[:32].hex())
                      open('/tmp/mengman-detail/'+name,'wb').write(data)
              except Exception as e:
                  print(path,'ERROR',repr(e))
          PY
          ls -lh /tmp/mengman-detail
          sha256sum /tmp/mengman-detail/* || true

      - name: Upload detail data
        uses: actions/upload-artifact@v4
        with:
          name: mengman-detail-data
          path: /tmp/mengman-detail
          if-no-files-found: error
          retention-days: 1
