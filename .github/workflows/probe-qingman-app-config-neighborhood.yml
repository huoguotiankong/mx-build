name: Map Qingman AppConfig Fetch Helpers

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/probe-qingman-app-config-neighborhood.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Prepare analyzer
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq python3-pyelftools python3-capstone cmake ninja-build build-essential pkg-config libicu-dev libcapstone-dev >/dev/null
          git clone -q --depth=1 https://github.com/worawit/blutter.git /tmp/blutter

      - name: Restore analyzer cache
        uses: actions/cache@v4
        with:
          path: |
            /tmp/blutter/bin
            /tmp/blutter/dartsdk
            /tmp/blutter/packages
          key: qingman-blutter-dart-3.12.2-arm64-v1

      - name: Map config schema and helper call graph
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY' > /tmp/apk_url
          import json,urllib.request,sys
          o=json.load(urllib.request.urlopen('https://bass8ycl.zermh.com/api/radio/api/config/version',timeout=20))
          a=o['data']['datas']['android']
          print(a['downloadUrl'])
          print('OFFICIAL_VERSION='+str(a.get('version','')),file=sys.stderr)
          PY
          curl -fsSL --retry 2 --connect-timeout 20 --max-time 180 "$(head -1 /tmp/apk_url)" -o /tmp/qingman.apk
          rm -rf /tmp/app-lib /tmp/blutter-out /tmp/apk-extract
          mkdir -p /tmp/app-lib /tmp/blutter-out /tmp/apk-extract
          unzip -q /tmp/qingman.apk 'lib/arm64-v8a/libapp.so' 'lib/arm64-v8a/libflutter.so' -d /tmp/apk-extract
          cp /tmp/apk-extract/lib/arm64-v8a/libapp.so /tmp/app-lib/
          cp /tmp/apk-extract/lib/arm64-v8a/libflutter.so /tmp/app-lib/
          cd /tmp/blutter
          python3 blutter.py /tmp/app-lib /tmp/blutter-out >/tmp/blutter.log 2>&1

          python3 - <<'PY'
          from pathlib import Path
          import re
          from capstone import Cs,CS_ARCH_ARM64,CS_MODE_ARM
          from elftools.elf.elffile import ELFFile

          pp={}
          for line in Path('/tmp/blutter-out/pp.txt').read_text('utf-8',errors='replace').splitlines():
              m=re.match(r'\[pp\+0x([0-9a-fA-F]+)\]\s+(.*)$',line)
              if m: pp[int(m.group(1),16)]=m.group(2)

          sensitive=re.compile(r'(?i)(token|secret|password|cookie|authorization|bearer|private|clientp12|certificate|proof|signature|sign/key|k1a|k1b|k2a|k2b|k3a|k3b|seed)')
          print('=== APP CONFIG OBJECT POOL 0x1ea00-0x1ed80 ===')
          for off in sorted(k for k in pp if 0x1ea00 <= k <= 0x1ed80):
              v=pp[off]
              if sensitive.search(v):
                  print(f'PP+0x{off:x} <redacted-sensitive-entry>')
                  continue
              print(f'PP+0x{off:x} {v[:500]}')

          # Also print safe names around call-site receiver/type offsets used by AppConfig fetch.
          print('=== SAFE RECEIVER/TYPE OFFSETS ===')
          for off in (0x5ba8,0x6fd0,0x6d18,0x7018,0x205c0,0x1ca78,0x1ca80,0x1ca88,0x1ca90,0x1c9a0,0x1c9b0,0x194c8,0x174f8,0x1ad0,0x7da8):
              v=pp.get(off,'<missing>')
              if sensitive.search(v): v='<redacted-sensitive-entry>'
              print(f'PP+0x{off:x} {v[:500]}')

          lib='/tmp/app-lib/libapp.so'
          with open(lib,'rb') as f:
              elf=ELFFile(f)
              sec=elf.get_section_by_name('.text')
              base=int(sec['sh_addr']); data=sec.data()
          md=Cs(CS_ARCH_ARM64,CS_MODE_ARM); md.skipdata=True

          # Helper functions called by the config flow. Dump compact branch/PP traces only.
          helpers=[
              ('config_decode_or_parse',0x4cfcfc,0x4cfd18),
              ('config_alt_parse',0x4cf9a0,0x4cfb10),
              ('config_model_decode',0x4cfb10,0x4cfcfc),
              ('fetch_app_config',0xb028dc,0xb02b3c),
              ('parse_app_config_result',0xb02664,0xb02828),
              ('config_error_helper',0xb02b3c,0xb02c18),
          ]
          for name,start,end in helpers:
              print(f'\n=== HELPER {name} {start:#x}-{end:#x} ===')
              if start < base or end > base+len(data):
                  print('OUT_OF_TEXT'); continue
              blob=data[start-base:end-base]
              pending={}
              for ins in md.disasm(blob,start):
                  pending={r:(b,t-1) for r,(b,t) in pending.items() if t>1}
                  op=ins.op_str.lower(); ann=''
                  m=re.match(r'(x\d+),\s*x27,\s*#(0x[0-9a-f]+|\d+)(?:,\s*lsl\s*#(\d+))?$',op)
                  if ins.mnemonic=='add' and m:
                      pending[m.group(1)]=(int(m.group(2),0)<<int(m.group(3) or 0),12)
                  elif ins.mnemonic in ('ldr','ldur','ldrsw'):
                      m=re.match(r'([wx]\d+),\s*\[(x\d+)(?:,\s*#(-?(?:0x[0-9a-f]+|\d+)))?\]',op)
                      if m:
                          br=m.group(2); disp=int(m.group(3),0) if m.group(3) else 0
                          off=disp if br=='x27' else (pending[br][0]+disp if br in pending else None)
                          if off is not None:
                              val=pp.get(off,'')
                              if sensitive.search(val):
                                  ann=f' PP+0x{off:x}=<redacted>'
                              else:
                                  ann=f' PP+0x{off:x}' + (f' [{val[:180]}]' if val else '')
                  if ann or ins.mnemonic in ('bl','blr','b','ret','cbz','cbnz','tbz','tbnz','b.eq','b.ne','b.ls','b.hi'):
                      print(f'{ins.address:#x} {ins.mnemonic:<7} {ins.op_str}{ann}')
          PY
