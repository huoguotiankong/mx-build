name: Trace Qingman Request Metadata Dispatch

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/probe-qingman-config-descriptor-classes.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Prepare analyzer
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq python3-pyelftools python3-capstone cmake ninja-build build-essential pkg-config libicu-dev libcapstone-dev >/dev/null
          git clone -q --depth=1 https://github.com/worawit/blutter.git /tmp/blutter

      - name: Restore analyzer cache
        uses: actions/cache@v4
        with:
          path: |
            /tmp/blutter/bin
            /tmp/blutter/dartsdk
            /tmp/blutter/packages
          key: qingman-blutter-dart-3.12.2-arm64-v1

      - name: Trace constructor, QxNet dispatch, and public config route candidates
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY' > /tmp/apk_url
          import json,urllib.request,sys
          o=json.load(urllib.request.urlopen('https://bass8ycl.zermh.com/api/radio/api/config/version',timeout=20))
          a=o['data']['datas']['android']
          print(a['downloadUrl'])
          print('OFFICIAL_VERSION='+str(a.get('version','')),file=sys.stderr)
          PY
          curl -fsSL --retry 2 --connect-timeout 20 --max-time 180 "$(head -1 /tmp/apk_url)" -o /tmp/qingman.apk
          rm -rf /tmp/app-lib /tmp/blutter-out /tmp/apk-extract
          mkdir -p /tmp/app-lib /tmp/blutter-out /tmp/apk-extract
          unzip -q /tmp/qingman.apk 'lib/arm64-v8a/libapp.so' 'lib/arm64-v8a/libflutter.so' -d /tmp/apk-extract
          cp /tmp/apk-extract/lib/arm64-v8a/libapp.so /tmp/app-lib/
          cp /tmp/apk-extract/lib/arm64-v8a/libflutter.so /tmp/app-lib/
          cd /tmp/blutter
          python3 blutter.py /tmp/app-lib /tmp/blutter-out >/tmp/blutter.log 2>&1

          python3 - <<'PY'
          from pathlib import Path
          import re
          from capstone import Cs,CS_ARCH_ARM64,CS_MODE_ARM
          from elftools.elf.elffile import ELFFile

          sensitive=re.compile(r'(?i)(token|secret|password|cookie|authorization|bearer|private|clientp12|certificate|proof|signature|sign/key|k1a|k1b|k2a|k2b|k3a|k3b|seed)')
          pp={}
          pp_lines=Path('/tmp/blutter-out/pp.txt').read_text('utf-8',errors='replace').splitlines()
          for line in pp_lines:
              m=re.match(r'\[pp\+0x([0-9a-fA-F]+)\]\s+(.*)$',line)
              if m: pp[int(m.group(1),16)]=m.group(2)

          def safe(v):
              if not v: return ''
              if sensitive.search(v): return '<redacted-sensitive>'
              return v[:320]

          with open('/tmp/app-lib/libapp.so','rb') as f:
              elf=ELFile=ELFFile(f)
              sec=elf.get_section_by_name('.text')
              base=int(sec['sh_addr']); data=sec.data()
          md=Cs(CS_ARCH_ARM64,CS_MODE_ARM); md.skipdata=True

          def dump(start,end):
              pending={}
              for ins in md.disasm(data[start-base:end-base],start):
                  pending={r:(b,n-1) for r,(b,n) in pending.items() if n>1}
                  op=ins.op_str.lower(); ann=''
                  m=re.match(r'(x\d+),\s*x27,\s*#(0x[0-9a-f]+|\d+)(?:,\s*lsl\s*#(\d+))?$',op)
                  if ins.mnemonic=='add' and m:
                      pending[m.group(1)]=(int(m.group(2),0)<<int(m.group(3) or 0),18)
                  elif ins.mnemonic in ('ldr','ldur','ldrsw'):
                      m=re.match(r'([wx]\d+),\s*\[(x\d+)(?:,\s*#(-?(?:0x[0-9a-f]+|\d+)))?\]',op)
                      if m:
                          br=m.group(2); disp=int(m.group(3),0) if m.group(3) else 0
                          off=disp if br=='x27' else (pending[br][0]+disp if br in pending else None)
                          if off is not None and off in pp:
                              ann=f' ; PP+0x{off:x}={safe(pp[off])}'
                  print(f'{ins.address:#x} {ins.mnemonic:<8} {ins.op_str}{ann}')

          print('=== COMMON REQUEST CONSTRUCTOR 0x3ea090 ===')
          dump(0x3ea090,0x3ea340)
          print('=== SHARED QXNET DISPATCH 0xaa0250 ===')
          dump(0xaa0250,0xaa0a80)

          print('=== CONFIG/APP ROUTE STRING CANDIDATES ===')
          route_re=re.compile(r'(?i)(?:api/[^" ]*(?:config|setting|version|startup|init)[^" ]*|[^" ]*(?:appconfig|chaping)[^" ]*)')
          for off,val in sorted(pp.items()):
              if sensitive.search(val): continue
              if route_re.search(val):
                  print(f'PP+0x{off:x} {val[:500]}')

          print('=== APP/CHAPING MODEL NEIGHBORHOODS ===')
          for needle in ('TypeArguments: <uxb?>','TypeArguments: <Bsb?>','Get app config failed','Get chaping config failed'):
              hits=[i for i,s in enumerate(pp_lines) if needle.lower() in s.lower()]
              print(f'NEEDLE {needle} hits={len(hits)}')
              seen=set()
              for i in hits[:8]:
                  for n in range(max(0,i-90),min(len(pp_lines),i+120)):
                      if n in seen: continue
                      seen.add(n)
                      s=pp_lines[n]
                      if sensitive.search(s): s='<redacted-sensitive-line>'
                      print(f'{n+1}: {s[:600]}')
          PY
