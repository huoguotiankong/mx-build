name: Map Qingman Dvb Reader Type

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/probe-qingman-dvb-type-map.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - name: Prepare analyzer
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq python3-pyelftools python3-capstone cmake ninja-build build-essential pkg-config libicu-dev libcapstone-dev >/dev/null
          git clone -q --depth=1 https://github.com/worawit/blutter.git /tmp/blutter
      - uses: actions/cache@v4
        with:
          path: |
            /tmp/blutter/bin
            /tmp/blutter/dartsdk
            /tmp/blutter/packages
          key: qingman-blutter-dart-3.12.2-arm64-v1
      - name: Locate Dvb declarations and functions
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY' >/tmp/apk_url
          import json,urllib.request
          print(json.load(urllib.request.urlopen('https://bass8ycl.zermh.com/api/radio/api/config/version',timeout=20))['data']['datas']['android']['downloadUrl'])
          PY
          curl -fsSL --retry 2 --connect-timeout 20 --max-time 180 "$(cat /tmp/apk_url)" -o /tmp/qingman.apk
          rm -rf /tmp/app-lib /tmp/blutter-out /tmp/apk-extract
          mkdir -p /tmp/app-lib /tmp/blutter-out /tmp/apk-extract
          unzip -q /tmp/qingman.apk 'lib/arm64-v8a/libapp.so' 'lib/arm64-v8a/libflutter.so' -d /tmp/apk-extract
          cp /tmp/apk-extract/lib/arm64-v8a/libapp.so /tmp/app-lib/
          cp /tmp/apk-extract/lib/arm64-v8a/libflutter.so /tmp/app-lib/
          cd /tmp/blutter
          python3 blutter.py /tmp/app-lib /tmp/blutter-out >/tmp/blutter.log 2>&1
          python3 - <<'PY'
          from pathlib import Path
          import re
          root=Path('/tmp/blutter-out')
          sensitive=re.compile(r'(?i)(password|passwd|pwd|cookie|authorization|bearer|private|clientp12|certificate|p12|seed)')
          needles=('Dvb','Vtb','Kvb','YBm','NCm')
          print('=== DECLARATION / CLASS HITS ===')
          count=0
          for p in root.rglob('*.dart'):
              lines=p.read_text('utf-8',errors='replace').splitlines()
              hitidx=[i for i,s in enumerate(lines) if any(n in s for n in needles)]
              if not hitidx: continue
              print(f'--- {p} ---')
              for i in hitidx[:25]:
                  for s in lines[max(0,i-3):min(len(lines),i+7)]:
                      if sensitive.search(s): continue
                      print(s[:1200])
                  print('...')
                  count+=1
                  if count>=70: raise SystemExit
          print('\n=== PP TYPE HITS ===')
          for line in root.joinpath('pp.txt').read_text('utf-8',errors='replace').splitlines():
              if sensitive.search(line): continue
              if any(n in line for n in needles): print(line[:1200])
          PY
