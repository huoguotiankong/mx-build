name: JMComic Plus Private Main Build

on:
  push:
    branches: [main]
    paths:
      - ".github/jmplus-bootstrap-handoff-trigger"
      - ".github/workflows/jmplus-bootstrap-handoff.yml"

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check private source token
        shell: bash
        env:
          MX_PRIVATE_READ_TOKEN: ${{ secrets.MX_PRIVATE_READ_TOKEN }}
        run: |
          set -euo pipefail
          [[ -n "$MX_PRIVATE_READ_TOKEN" ]] || { echo "::error::Missing MX_PRIVATE_READ_TOKEN"; exit 1; }

      - name: Checkout private mx-dev main
        uses: actions/checkout@v5
        with:
          repository: huoguotiankong/mx-dev
          ref: main
          token: ${{ secrets.MX_PRIVATE_READ_TOKEN }}
          path: source
          fetch-depth: 1
          persist-credentials: false

      - name: Verify requested feature contract
        shell: bash
        run: |
          set -euo pipefail
          SRC="source/src/zh/jmcomicplus/src/eu/kanade/tachiyomi/extension/zh/jmcomicplus/JmComicPlus.kt"
          test -f source/src/zh/jmcomicplus/build.gradle.kts
          test -f "$SRC"
          grep -q 'CommentCapabilities(true, true' "$SRC"
          grep -q 'getMangaCommentTarget' "$SRC"
          grep -q 'getChapterCommentTarget' "$SRC"
          grep -q 'CommentTargetKind.CHAPTER' "$SRC"
          if grep -Eqi 'paragraph(comment|review)|segment(comment|review)|段评' "$SRC"; then
            echo "::error::Unexpected paragraph-comment implementation found"
            exit 1
          fi
          grep -q 'versionCode = 2' source/src/zh/jmcomicplus/build.gradle.kts
          git -C source rev-parse HEAD

      - name: Validate mx-dev source layout
        working-directory: source
        run: python3 scripts/validate_sources.py

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Checkout current Keiyoushi framework
        uses: actions/checkout@v5
        with:
          repository: keiyoushi/extensions-source
          path: upstream
          fetch-depth: 1
          persist-credentials: false

      - name: Overlay actual private JMComic Plus module
        shell: bash
        run: |
          set -euo pipefail
          rm -rf upstream/src/zh/jmcomicplus
          cp -a source/src/zh/jmcomicplus upstream/src/zh/jmcomicplus

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Spotless check
        working-directory: upstream
        run: ./gradlew :src:zh:jmcomicplus:spotlessCheck

      - name: Build Debug extension
        working-directory: upstream
        run: ./gradlew :src:zh:jmcomicplus:assembleDebug --stacktrace
