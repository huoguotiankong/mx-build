name: Trace Qingman AppConfig Request Base

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/probe-qingman-request-base.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trace:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - name: Prepare analyzer
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq python3-pyelftools python3-capstone cmake ninja-build build-essential pkg-config libicu-dev libcapstone-dev >/dev/null
          git clone -q --depth=1 https://github.com/worawit/blutter.git /tmp/blutter
      - name: Restore analyzer cache
        uses: actions/cache@v4
        with:
          path: |
            /tmp/blutter/bin
            /tmp/blutter/dartsdk
            /tmp/blutter/packages
          key: qingman-blutter-dart-3.12.2-arm64-v1
      - name: Map AppConfig route neighborhood
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY' >/tmp/apk_url
          import json,urllib.request
          a=json.load(urllib.request.urlopen('https://bass8ycl.zermh.com/api/radio/api/config/version',timeout=20))['data']['datas']['android']
          if str(a.get('version'))!='4.4.5' or str(a.get('versionCode'))!='445': raise SystemExit('Unexpected Qingman version')
          print(a['downloadUrl'])
          PY
          curl -fsSL --retry 2 --connect-timeout 20 --max-time 180 "$(cat /tmp/apk_url)" -o /tmp/qingman.apk
          rm -rf /tmp/app-lib /tmp/blutter-out /tmp/apk-extract
          mkdir -p /tmp/app-lib /tmp/blutter-out /tmp/apk-extract
          unzip -q /tmp/qingman.apk 'lib/arm64-v8a/libapp.so' 'lib/arm64-v8a/libflutter.so' -d /tmp/apk-extract
          cp /tmp/apk-extract/lib/arm64-v8a/libapp.so /tmp/app-lib/
          cp /tmp/apk-extract/lib/arm64-v8a/libflutter.so /tmp/app-lib/
          cd /tmp/blutter
          python3 blutter.py /tmp/app-lib /tmp/blutter-out >/tmp/blutter.log 2>&1
          python3 - <<'PY'
          from pathlib import Path
          from capstone import Cs,CS_ARCH_ARM64,CS_MODE_ARM
          from elftools.elf.elffile import ELFFile
          import re

          deny=re.compile(r'(?i)(password|token|authorization|bearer|cookie|secret|private|clientp12|certificate|keystore|proof|signature|sign/key|seed|k1a|k1b|k2a|k2b|k3a|k3b)')
          pplines=Path('/tmp/blutter-out/pp.txt').read_text('utf-8',errors='replace').splitlines()
          pp={}
          for line in pplines:
              m=re.match(r'\[pp\+0x([0-9a-fA-F]+)\]\s+(.*)$',line)
              if m: pp[int(m.group(1),16)]=m.group(2)

          print('=== PP WINDOW AROUND APPCONFIG / BASEIMGURL ===')
          anchors=[off for off,val in pp.items() if 'AppConfig' in val or 'baseImgUrl' in val]
          for anchor in sorted(set(anchors)):
              print(f'-- anchor=0x{anchor:x} {pp[anchor][:300]} --')
              for off,val in sorted(pp.items()):
                  if anchor-0x900 <= off <= anchor+0x900 and not deny.search(val):
                      low=val.lower()
                      if ('string:' in low or 'field <' in low or 'type:' in low or 'closure:' in low or 'function' in low):
                          print(f'PP+0x{off:x} {val[:420]}')

          print('=== CLASS 992 / TXB METADATA ===')
          for name in ('objs.txt','pp.txt'):
              p=Path('/tmp/blutter-out')/name
              lines=p.read_text('utf-8',errors='replace').splitlines()
              for i,z in enumerate(lines):
                  if re.search(r'\btxb\b',z,re.I) or re.search(r'class id:\s*992\b',z,re.I) or re.search(r'cid[=: ]+992\b',z,re.I):
                      print(f'FILE={name} LINE={i+1}')
                      for q in lines[max(0,i-20):min(len(lines),i+60)]:
                          if not deny.search(q): print(q[:700])

          for p in Path('/tmp/blutter-out/asm').glob('*.dart'):
              lines=p.read_text('utf-8',errors='replace').splitlines()
              for i,z in enumerate(lines):
                  if re.match(r'class\s+txb\b',z.strip()):
                      print(f'=== TXB ASM FILE={p.name} LINE={i+1} ===')
                      for q in lines[max(0,i-35):min(len(lines),i+100)]:
                          if not deny.search(q): print(q[:800])

          with open('/tmp/app-lib/libapp.so','rb') as f:
              elf=ELFFile(f); sec=elf.get_section_by_name('.text'); base=int(sec['sh_addr']); data=sec.data()
          md=Cs(CS_ARCH_ARM64,CS_MODE_ARM); md.skipdata=True

          def dump_full(start,end,title):
              print(f'=== {title} 0x{start:x}-0x{end:x} ===')
              pending={}
              for ins in md.disasm(data[start-base:end-base],start):
                  pending={r:(b,n-1) for r,(b,n) in pending.items() if n>1}
                  op=ins.op_str.lower(); ann=''
                  m=re.match(r'(x\d+),\s*x27,\s*#(0x[0-9a-f]+|\d+)(?:,\s*lsl\s*#(\d+))?$',op)
                  if ins.mnemonic=='add' and m:
                      pending[m.group(1)]=(int(m.group(2),0)<<int(m.group(3) or 0),48)
                  elif ins.mnemonic in ('ldr','ldur','ldrsw'):
                      m=re.match(r'([wx]\d+),\s*\[(x\d+)(?:,\s*#(-?(?:0x[0-9a-f]+|\d+)))?\]',op)
                      if m:
                          br=m.group(2); disp=int(m.group(3),0) if m.group(3) else 0
                          off=disp if br=='x27' else (pending[br][0]+disp if br in pending else None)
                          if off is not None and off in pp and not deny.search(pp[off]): ann=f' ; PP+0x{off:x}={pp[off][:300]}'
                  # Full caller instructions are safe; only suppress annotations containing denied material.
                  print(f'0x{ins.address:x} {ins.mnemonic:<7} {ins.op_str}{ann}')

          dump_full(0xb028dc,0xb02b60,'APPCONFIG FETCH FULL')
          dump_full(0xb02b90,0xb02bc8,'TXB ALLOC STUB')

          print('=== FUNCTIONS/CALLERS NEAR APPCONFIG FETCH ===')
          for p in Path('/tmp/blutter-out/asm').glob('*.dart'):
              lines=p.read_text('utf-8',errors='replace').splitlines()
              hits=[i for i,z in enumerate(lines) if '0xb028dc' in z.lower() or '0xb0293c' in z.lower() or '0xb09d44' in z.lower()]
              for h in hits[:8]:
                  print(f'FILE={p.name} HIT={h+1}')
                  for q in lines[max(0,h-80):min(len(lines),h+260)]:
                      if not deny.search(q): print(q[:800])
          PY
